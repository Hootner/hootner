#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🦉 HOOTNER - Running pre-push checks..."

# Run smoke tests
npm run test:smoke

if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Push aborted."
  exit 1
fi

echo "✅ Pre-push checks passed!"
